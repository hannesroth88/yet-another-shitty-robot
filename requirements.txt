# Phase 0 prototype deps (Mac host). LLM uses Ollama HTTP (no client lib needed).
faster-whisper>=1.0.0
# Optional cross-platform TTS (fleet default). On macOS, the `say` backend needs
# nothing; install piper-tts on Linux fleet hosts or use a prebuilt piper binary.
# piper-tts>=1.2.0

# Silero VAD (VAD_BACKEND=silero, now the default). Pulls in torch automatically.
# silero-vad>=5.1

# Optional: German Kokoro "Martin" TTS backend (TTS_BACKEND=kokoro)
# kokoro-onnx
# huggingface_hub

# Optional: Qwen3-TTS, Apple Silicon MLX path -- fast + voice clone (TTS_BACKEND=qwen3-mlx)
#   the recommended quality path on the M1 (RTF < 1, clones a reference voice)
# mlx-audio

# Optional: Qwen3-TTS, PyTorch/transformers path for x86/CUDA hosts (TTS_BACKEND=qwen3)
#   note: pins transformers==4.57.3, which conflicts with mlx-audio; use one venv per path
# qwen-tts
# torch
